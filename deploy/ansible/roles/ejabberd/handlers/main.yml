---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart ejabberd
  ansible.builtin.systemd_service:
    name: ejabberd
    state: restarted
    daemon_reload: true
