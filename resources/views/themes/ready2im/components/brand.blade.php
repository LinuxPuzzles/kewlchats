{{-- ready2.im wordmark. Outer color is inherited (set by the parent, e.g. text-white on
     the blue nav/page); the ".im" accent is always lime. --}}
<span {{ $attributes->merge(['class' => 'font-extrabold italic tracking-tight']) }}>ready2<span class="text-lime-300">.im</span></span>
